# Both Vite development and Vercel rewrite this same-origin path to the n8n webhook.
VITE_SCHEDULE_API_URL=/api/schedule

# Optional until the n8n chat workflow URL is available.
# Expected contract: POST { message, sessionId } -> { reply }
VITE_CHAT_API_URL=
