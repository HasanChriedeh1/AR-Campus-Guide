# BootCamp Workspace

- This is a Vite React TypeScript application.
- Use npm scripts from `package.json` for development and validation.
- Keep changes focused and preserve the existing Vite structure.
- Run `npm run build` after implementation changes.
